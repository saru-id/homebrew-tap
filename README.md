# saru-id/homebrew-tap

```sh
brew install saru-id/tap/jumon
```

Formulae for saru-id tools. Note: `jumon` installs from its GitHub
release tarball; until that repository is public, the download needs
access to it.
